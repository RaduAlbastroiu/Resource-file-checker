//=================
//Input reader cpp:
//=================

#include "stdafx.h"
#include "rect.h"
#include "Parsare_Input.h"
#include "Input_reader.h"

using namespace std;
/*
rect Input_reader::Take_line(ifstream &fin)
{
	rect OBJ;
	char Line[Length_read_line];
	int i = 0, Line_length;

	fin.get(Line, Length_read_line - 1);

	Line_length = strlen(Line);

	if (Line[0] == 'E' && Line[1] == 'N' && Line[2] == 'D')
	{
		OBJ.Change_RECT_VALIDATION(false);
		//stop;
	}
	else
	{
		OBJ = parsare_obj.Parsare::Parsare_line(Line);
	}

	return OBJ;
}
*/
//=================
//modificare totala
